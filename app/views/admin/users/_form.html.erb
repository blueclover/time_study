<%= simple_form_for [:admin, @user] do |f| %>
  <%= f.error_notification %>

	<%= f.association :county %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :admin %>

  <div class="form-actions">
		<%= f.submit %>
	</div>
<% end %>