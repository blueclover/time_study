<div class='span10'>
	<h3><%= @log_entry.date.strftime('%A, %B %e, %Y') %></h3>
	<div class="span8" id="activities">
		<table class="table">
			<tbody>
				<% @activities.each do |activity| %>
					<tr>
						<td><%= activity.code %></td>
						<td><%= activity.name %></td>
						<td><%= activity.hours || 0 %></td>
					</tr>
				<% end -%>
				<tr>
					<td></td>
					<td>Other</td>
					<td><%= 8 - @log_entry.total_hours || 0 %></td>
				</tr>
			</tbody>
		</table>
	</div>
	<p>
		<%= link_to "Edit", [:edit, @activity_log, @log_entry], 
								class: 'btn' if @log_entry.date < Date.tomorrow %>
		<%= link_to "View History", [@activity_log.survey, @activity_log], class: "btn" %>
	</p>
</div>